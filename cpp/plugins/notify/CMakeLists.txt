ADD_DEFINITIONS(-DENABLE_DEBUG=1)

INCLUDE_DIRECTORIES(
  ${PROJECT_BINARY_DIR}
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_SOURCE_DIR}/include
  ${Boost_INCLUDE_DIR}
  ${CURL_INCLUDE_DIRS}
  ${GLIBMM_INCLUDE_DIRS}
  ${GTKMM_INCLUDE_DIRS}
  ${SOUP_INCLUDE_DIRS}
  ${XML_INCLUDE_DIRS}
  ${NOTIFY_INCLUDE_DIRS}
  ${NM_INCLUDE_DIRS}
)

SET(cppmod_notify_SOURCES
  cppmod-notify.cc
  cppmod-notify.hh
)

ADD_LIBRARY(cppmod-notify MODULE ${cppmod_notify_SOURCES})

TARGET_LINK_LIBRARIES(cppmod-notify
  ${CURL_LIBRARIES}
  ${GLIBMM_LIBRARIES}
  ${GTKMM_LIBRARIES}
  ${SOUP_LIBRARIES}
  ${NOTIFY_LIBRARIES}
  ${NM_LIBRARIES}
)

INSTALL(TARGETS cppmod-notify LIBRARY DESTINATION ${MPX_PLUGIN_DIR}/cppmod)
